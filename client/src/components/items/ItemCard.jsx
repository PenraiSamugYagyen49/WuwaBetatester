export default function ItemCard({ item, onNavigate }) { const open = () => onNavigate(`/items/${item.slug}`); return <article className={`item-card rarity-${item.rarity ?? 0}`} tabIndex="0" role="link" onClick={open} onKeyDown={event => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); open(); } }}><div className="item-card__image"><img src={item.image} alt="" loading="lazy" onError={event => { event.currentTarget.style.display = 'none'; event.currentTarget.nextElementSibling.hidden = false; }} /><span hidden aria-hidden="true">◇</span></div><small>{item.category}</small><h2>{item.name}</h2>{item.rarity && <p>{'★'.repeat(item.rarity)}</p>}</article>; }
